# 📖 如何運行 run_custom_story.py - 粵語完整指南

## 🎯 運行步驟（按順序）

### **步驟 1️⃣：打開終端機 (Terminal)**

如果你用緊 **WSL (Windows Subsystem for Linux)**：
- 打開 WSL 終端機
- 或者用 Windows Terminal

如果你用緊 **Windows PowerShell**：
- 打開 PowerShell 或 CMD

---

### **步驟 2️⃣：進入正確嘅目錄**

```bash
cd /mnt/c/Users/user/Desktop/ming/ming
```

或者如果你已經喺 `ming` 目錄：
```bash
cd ming
```

**檢查你喺邊度：**
```bash
pwd
```
應該顯示：`/mnt/c/Users/user/Desktop/ming/ming`

---

### **步驟 3️⃣：檢查 Python 是否安裝**

**WSL 環境：**
```bash
python3 --version
```
或者
```bash
python --version
```

**Windows PowerShell 環境：**
```powershell
python --version
```
或者
```powershell
py --version
```

如果顯示版本號（例如 `Python 3.9.x`），就代表安裝咗 ✅

---

### **步驟 4️⃣：檢查故事檔案是否存在**

**WSL 環境：**
```bash
ls -la my_custom_story.txt
```
或者
```bash
ls -la ../my_custom_story.txt
```

**Windows PowerShell：**
```powershell
dir my_custom_story.txt
```
或者
```powershell
dir ..\my_custom_story.txt
```

應該要見到你嘅故事檔案 ✅

---

### **步驟 5️⃣：運行腳本**

#### **方法 A：WSL 環境（推薦）**

```bash
python3 ming/run_custom_story.py
```

或者如果你已經喺 `ming` 目錄入面：
```bash
python3 run_custom_story.py
```

#### **方法 B：Windows PowerShell**

```powershell
python ming\run_custom_story.py
```

或者如果你已經喺 `ming` 目錄入面：
```powershell
python run_custom_story.py
```

#### **方法 C：用完整路徑（如果上面都唔得）**

**WSL：**
```bash
python3 /mnt/c/Users/user/Desktop/ming/ming/run_custom_story.py
```

**Windows PowerShell：**
```powershell
python C:\Users\user\Desktop\ming\ming\run_custom_story.py
```

---

## 🔧 常見問題解決方法

### **問題 1：找不到 Python**

**WSL 環境：**
```bash
# 安裝 Python3（如果未安裝）
sudo apt update
sudo apt install python3 python3-pip
```

**Windows：**
- 去 [python.org](https://www.python.org/downloads/) 下載安裝
- 安裝時記得勾選 "Add Python to PATH"

---

### **問題 2：找不到模組 (Module not found)**

**解決方法：安裝依賴**
```bash
cd /mnt/c/Users/user/Desktop/ming
pip3 install -r requirements.txt
```

或者
```bash
pip install -r requirements.txt
```

---

### **問題 3：找不到故事檔案**

**檢查檔案位置：**
```bash
# WSL
find /mnt/c/Users/user/Desktop/ming -name "my_custom_story.txt"

# Windows PowerShell
Get-ChildItem -Path C:\Users\user\Desktop\ming -Filter "my_custom_story.txt" -Recurse
```

**如果檔案喺根目錄：**
- 確保檔案喺：`C:\Users\user\Desktop\ming\my_custom_story.txt`
- 或者：`C:\Users\user\Desktop\ming\ming\my_custom_story.txt`

---

### **問題 4：權限錯誤 (Permission denied)**

**WSL：**
```bash
chmod +x ming/run_custom_story.py
```

**Windows：**
- 通常唔會有權限問題
- 如果係，用管理員身份運行 PowerShell

---

## 📋 完整運行流程（一次過）

### **WSL 環境：**

```bash
# 1. 進入目錄
cd /mnt/c/Users/user/Desktop/ming/ming

# 2. 檢查 Python
python3 --version

# 3. 檢查故事檔案
ls -la my_custom_story.txt || ls -la ../my_custom_story.txt

# 4. 運行腳本
python3 run_custom_story.py
```

### **Windows PowerShell：**

```powershell
# 1. 進入目錄
cd C:\Users\user\Desktop\ming\ming

# 2. 檢查 Python
python --version

# 3. 檢查故事檔案
dir my_custom_story.txt
# 或者
dir ..\my_custom_story.txt

# 4. 運行腳本
python run_custom_story.py
```

---

## 🎯 快速測試命令

**測試腳本是否可以運行：**
```bash
python3 -c "import sys; print(sys.version)"
```

**測試模組是否安裝：**
```bash
python3 -c "from custom_story_reader import read_custom_story; print('OK')"
```

---

## ✅ 成功運行後會見到：

```
=== 📝 自定義故事圖片生成（無需 OpenAI） ===
✅ 找到故事檔案：/mnt/c/Users/user/Desktop/ming/my_custom_story.txt
✅ Telegram 環境初始化成功
🔍 驗證故事格式...
✅ 成功讀取故事
📰 標題: 準備結婚，男朋友話連「禮金」都要同我AA制...
🎭 視角檢測結果:
   📁 檔案名稱檢測: female (👩 女)
   📝 內容檢測: female (👩 女)
   ✅ 最終選擇: female (👩‍💼 女性視角 Girl View)
   🔍 證據: 發現「男朋友」→ 確認為女性視角 ✓
...
✅ 成功生成 9 張圖片
✅ 已發送到 Telegram
```

---

## 💡 提示

1. **確保你喺正確目錄**：腳本需要喺 `ming/` 目錄入面運行
2. **檢查故事檔案**：確保 `my_custom_story.txt` 存在
3. **檢查依賴**：確保所有 Python 模組都安裝咗
4. **用 Python3**：WSL 環境建議用 `python3` 而唔係 `python`

---

## 🆘 如果都係唔得

**檢查錯誤訊息：**
- 複製完整錯誤訊息
- 檢查係咪路徑問題
- 檢查係咪模組缺失

**最簡單方法：**
```bash
cd /mnt/c/Users/user/Desktop/ming/ming
python3 run_custom_story.py
```

如果都係唔得，請提供完整錯誤訊息！

